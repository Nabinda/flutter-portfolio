import 'package:flutter/material.dart';

class OnMenuItemSelected extends StatefulWidget {
  const OnMenuItemSelected({Key? key}) : super(key: key);

  @override
  _OnMenuItemSelectedState createState() => _OnMenuItemSelectedState();
}

class _OnMenuItemSelectedState extends State<OnMenuItemSelected> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
